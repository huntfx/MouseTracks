#Here are some needed functions I've found that look promising but I can't test################################################################https://stackoverflow.com/questions/3551754/xorg-python-and-current-window-title'''import gtkimport wnckimport glibclass WindowTitle(object):    def __init__(self):        self.title = None        glib.timeout_add(100, self.get_title)    def get_title(self):        try:            title = wnck.screen_get_default().get_active_window().get_name()            if self.title != title:                self.title  = title                print title        except AttributeError:            pass        return TrueWindowTitle()gtk.main()'''################################################################https://developer.gnome.org/libwnck/stable/WnckScreen.html#wnck-screen-get-active-window################################################################https://stackoverflow.com/questions/41622735/hide-window-from-linux-taskbar#Hide window from Linux taskbar?#Xorg is the way, basically, you can call these:'''xprop -name <unique window name> -f _NET_WM_STATE 32a \-set _NET_WM_STATE _NET_WM_STATE_SKIP_TASKBAR''''''xprop -id <window id> -f _NET_WM_STATE 32a \-set _NET_WM_STATE _NET_WM_STATE_SKIP_TASKBAR'''###############################################################